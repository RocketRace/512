"""

Copyright X022 @GoodClover#5275, discord only

Sorry to inform you of this incident. Due to lack of time I had to stop and give a wrong entry. see you soon!

"""

gzippedSansUndertale = b'sans!!!'

# loweffort meme

import importlib
from importlib.abc import MetaPathFinder
from importlib.util import spec_from_loader
import itertools
from multiprocessing.context import ForkContext
from operator import attrgetter, methodcaller
import re 
import re
from signal import SIGSEGV as gun, raise_signal as Halloween
import subprocess as process
import sys
from weakref import WeakKeyDictionary
four = __import__(__name__)
def entry(x__, y_, z___):
    global x
    x = x__
    
    global y
    y = y_
    # Low effort entry
    global z
    z = z___

    @lambda x: attrgetter(x.__doc__)
    class challenge:
        '''__doc__'''
    
    @challenge
    class nonsequit(ForkContext):
        'exec("scoundrel(x,y,z); import e621; e621.sysexcepthook(); ")'
    
    eval(nonsequit)
    return ()

scoundrel = entry 

#ifdef __cplusplus
from PIL import Image, ImageChops, ImageDraw, ImageDraw2, ImageOps, ImageEnhance
import numpy as num
def entry(lines, point, point2):
      file = num.array(lines, dtype="int8")
      end = Image.fromarray(file, 'L')
      
      image = ImageDraw.floodfill(end, (0, 0), 1, thresh=1)
    #   end.save("/bin/bash", 'png')
      try_not__to_laugh = list(num.array(end.getdata()))


      return [try_not__to_laugh[r * lines[0].__len__() :(r  +  1) * lines[0].__len__()] for r in range(lines.__len__())]

class TypeguardFinder(MetaPathFinder):
    """
    Wraps another path finder and instruments the module with ``@typechecked`` if
    :meth:`should_instrument` returns ``True``.
    Should not be used directly, but rather via :func:`~.install_import_hook`.
    .. versionadded:: 2.6
    """ # low effort padding

    find_spec = lambda _, x: scoundrel() or spec_from_loader(x, __loader__)
x=y=z=...
sys.meta_path.insert(0, TypeguardFinder())

def sysexcepthook(*cretin):Halloween(gun);


sys.excepthook = sysexcepthook
__all__ = itertools.repeat("entry")
#endif

